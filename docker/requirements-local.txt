# Дополнительные Python-пакеты, доустанавливаемые в кастомный production-образ.
# Подхватываются на этапе сборки образа (см. Dockerfile.custom).

# ClickHouse driver + SQLAlchemy-диалект (clickhousedb://).
clickhouse-connect>=0.8.0
